spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/myTasks
    username: postgres
    password: 123465
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  application:
    name: mytasks